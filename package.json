{
  "name": "ai-code-cleaner",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/core",
    "packages/languages/*",
    "packages/registry",
    "packages/vscode",
    "packages/cli"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check .",
    "format": "biome format --write .",
    "build:libs": "npm run build --workspace=@ai-code-cleaner/core && npm run build --workspace=@ai-code-cleaner/lang-typescript && npm run build --workspace=@ai-code-cleaner/lang-python && npm run build --workspace=@ai-code-cleaner/registry",
    "typecheck": "npm run build:libs && npm run typecheck --workspaces --if-present",
    "build": "npm run build:libs && npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^5.0.1"
  }
}
