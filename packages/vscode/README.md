# ai-code-cleaner-vscode

Pendiente de implementación (FASE 3). Ver `CLAUDE.md` en la raíz:
`src/extension.ts` (activation + comandos), `src/liveWatcher.ts` (modo automático),
`src/diffView.ts` (diff nativo de VS Code antes de aplicar).
