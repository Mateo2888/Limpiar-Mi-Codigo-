# @ai-code-cleaner/lang-typescript

Pendiente de implementación (FASE 3). Debe implementar `LanguageAdapter`
(`@ai-code-cleaner/core`) usando `web-tree-sitter` + gramáticas `tree-sitter-typescript`
y `tree-sitter-javascript`. Ver `docs/decisions.md` en la raíz del repo.
